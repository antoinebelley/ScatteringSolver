Code to compute 2-body amplitudes of 0vbb operators. To install dependencies run
```
conda env create -f environment.yaml
conda activate scattering
```

The file example.py will show how to compute the scattering wave function for a certain incoming and outgoing momentum. The file compute_amplitude_example.py
will compute the short- and long-range amplitude at different values of the regulator.
